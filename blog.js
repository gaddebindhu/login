// Optional JavaScript to toggle "Read More" button functionality
document.querySelectorAll('.read-more').forEach(button => {
    button.addEventListener('click', () => {
        alert('Redirecting to full post...');
        // You can replace this with actual redirection logic, for example:
        // window.location.href = '/full-post-url';
    });
});